apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java-app
  template:
    metadata:
      labels:
        app: node-app
    spec:
      containers:
        - name: java-container
          image: alaamohamed1/java-app:v10
          ports:
            - containerPort: 8090

